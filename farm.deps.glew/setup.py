
import os
from setuptools import setup

setup(
    name='farm.deps.glew',
    version='0.1.7',
    author='Me and myself',
    description='Farmers.',
    namespace_packages=['farm', 'farm.deps'],
    packages=['farm', 'farm.deps', 'farm.deps.glew'])
